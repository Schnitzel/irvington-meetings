{
  "name": "irvington-meetings",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "prepare-meeting": "node --experimental-strip-types scripts/prepare.ts",
    "renormalize": "node --experimental-strip-types scripts/renormalize.ts",
    "probe-gaps": "node --experimental-strip-types scripts/probe-gaps.ts",
    "suggest-speakers": "node --experimental-strip-types scripts/suggest-speakers.ts"
  },
  "dependencies": {
    "astro": "^7.3.2"
  }
}
